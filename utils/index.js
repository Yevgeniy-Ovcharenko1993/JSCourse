const arrays = require('./arrays');
const zipper = require('./zipper');
const differencer = require('./differencer');
const droper = require('./droper');

module.exports = {
  arrays,
  zipper,
  differencer,
  droper,
};
